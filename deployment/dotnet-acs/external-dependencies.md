# ACS 公共出口逐项目核对表

状态：2026-09-26 **只建立核对口径，当前生产生效配置和真实外呼结果尚未取得**。本表不包含密钥、证书内容、手机号或完整请求报文。`待核对` 是生产来源接入阻断项，不能根据源码中出现过某 SDK 就标成已启用。

每个实际启用的调用另建明细，字段固定为：`项目 / 角色 / 业务动作 / 配置来源和版本 / 目标域名:端口 / 协议与SDK版本 / 应用或商户标识引用 / 超时和重试 / 幂等业务键 / 回调URL / 旧新出口白名单负责人 / 供应商侧来源IP证据 / 测试关联ID和结果`。真实支付、退款、扣积分和短信测试在对象、金额或次数明确后再实施。

| 项目 | 纳入来源 | 公网调用重点 | 短信/任务及特殊门槛 | 生产生效配置、SDK 与来源 IP |
|---|---|---|---|---|
| DGYE | ACS API | 微信、支付及其他实际启用调用 | 旧直连短信通道全部禁用；检查恢复公网后的调度副作用 | 待核对 |
| VET | ACS API | 微信、支付及其他实际启用调用 | 同上 | 待核对 |
| STOPMP | ACS API | 微信、支付及其他实际启用调用 | 同上 | 待核对 |
| ETBST | ACS API | 微信、支付及其他实际启用调用 | 同上 | 待核对 |
| DDMP | ACS API | 微信、支付及其他实际启用调用 | 同上 | 待核对 |
| Yangu | ACS API | 微信、支付及其他实际启用调用 | 核对已启用任务状态与失败队列 | 待核对 |
| M1X | ACS API + Worker | 两角色分别登记实际调用 | `APP_ROLE`、唯一 Worker；两角色分别验收出口 | 待核对 |
| AI 自习室 | 拟迁 BackApi + Worker | 微信与实际启用接口 | ECS FrontApi 留用；共享 token、数据库及消息状态兼容 | 待核对，尚未迁移 |
| 售后工单 | 拟迁 FrontApi + BackApi + Worker | 聚水潭、飞书、支付与退款 | token 刷新、游标、幂等索引、Kestrel 端口 | 待核对，尚未迁移 |
| 积分商城 | 拟迁 FrontApi + BackApi + Worker | 三方积分、微信、支付退款、快递、地图、OCR | API 内 Redis 消费者单独交接 | 待核对，尚未迁移 |
| 新零售 | 拟迁 FrontApi + BackApi + Worker | 实际启用的支付、退款、分账 | Worker 无禁用调度参数时先保持零副本 | 待核对，尚未迁移 |
| 经销商查询 | 拟迁 FrontApi + BackApi | 必要地图/图片接口 | 不启用 Apollo 其他功能，无 Worker | 待核对，尚未迁移 |
| SmsCore | **不纳入**，保留 ECS | ECS → 短信供应商 | 项目私网访问 `172.27.182.18:3090`；核对 ECS 实际公网源 IP 与供应商白名单 | ECS 自带公网 IP `39.107.141.33` 已查，实际出口待验证 |
| AI FrontApi、以旧换新 | **不纳入**，保留 ECS | 维持现有公网路径 | 不随 .NET 项目迁移被停用 | 待核对其网络不受新路由影响 |

逐项目验收：先保存变更前 Ready、重启、入口和私网连通性；接入后分别验证真实 SDK 请求、供应商看到的固定 EIP、ALB Host/证书、私网数据库及 SmsCore、旧短信直连不再生效、Worker 和消费者无重复副作用。保留镜像 digest、源码 SHA、运行配置版本、白名单变更记录及 24 小时观察证据。旧新出口并行期间保留旧白名单，确认无旧流量后再按平台规则移除。

网络实施与回滚顺序见 [NAT 执行手册](egress-nat.md)，.NET 项目交付见 [发布记录](release-records.md)。
