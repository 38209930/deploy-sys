# 北京 ACS 部署交接与现状快照

核验时间：**2026-09-26 23:39 CST**。本页记录当时通过阿里云 OpenAPI、ACS Kubernetes API 和正式 HTTPS 入口读到的状态；状态会变化，接手后的第一步是重新读回。本文不包含生产配置值、密钥、手机号或客户数据。操作方法见 [阿里云 API 使用说明](aliyun-api-operations.md)，发布和故障流程见 [发布手册](runbook.md)、[运维备忘录](operations.md)。

**2026-09-27 00:30 更新**：管理员授权后，售后 Worker 已启动（1/1），Yangu、M1X 三个 Deployment 已迁入新出口网段并实测从固定 EIP 出公网；下表对应行已改，证据见[变更记录](worker-start-and-java-egress-2026-09-27.md)。

## 一分钟定位

| 对象 | 核验值 |
|---|---|
| 生产账号 / 地域 | `1442361567788059` / `cn-beijing`；CLI 操作显式用 `ruishi-prod-acr` Profile，默认 Profile 不是本生产账号 |
| ACS | `ruishi-prod-acs`，`cebc88343a44b4d759aa983a47b787835` |
| ACR | `cri-73ffxebpi6ruw6sn`；Java 镜像命名空间 `ruishi-java-prod`，.NET 镜像命名空间 `ruishi-dotnet-prod`。**ACR 命名空间不等于 ACS Namespace** |
| VPC / ALB | `vpc-2zervez1jgscsglpenrzo` / `alb-olyb9enxszy3f42nnn`；入口由各 Namespace 的精确 Host Ingress 接入 |
| 公共出口 | NAT `ngw-2zetnd6golba2sju2q7jo`，EIP `eip-2zeapte07xmvayr4852h5`（`39.96.67.239`，10 Mbps）；只为新 Pod 网段配置两条 SNAT，无 DNAT |
| 短信 | SmsCore 仍在 ECS `i-2ze38hdx1sufodad2kz0`，私网入口 `172.27.182.18:3090`；SmsCore 自身供应商出口不是 ACS NAT EIP |

## 当前运行清单

下表的副本和 Pod 重启数来自 ACS API。`/health/ready=200` 只代表本次 HTTPS 探测成功，不代表登录、支付、短信、Worker 实际业务已经验收。

| 项目 | ACS Deployment 期望/就绪 | 入口或保留旧端 | 本次证据与边界 |
|---|---|---|---|
| DGYE | `dgye-api` **0** | `dgye-api.svision100.com` 有 Ingress | 零副本；不可据 Ingress 判断可服务 |
| VET | `vet-api` **0** | `vet-api.svision100.com` 有 Ingress | 零副本 |
| STOPMP | `stopmp-api` **1/1** | `stop-mp-api.svision100.com` | 本次仅核对 ACS 控制面 |
| ETBST | `etbst-api` **1/1** | `et-bst-api.svision100.com` | 同上 |
| DDMP | `ddmp-api` **1/1** | `ddmpapi.svision100.com` | 同上 |
| Yangu | `yangu-api` **1/1** | `rsapi.svision100.com` | 已迁入新出口网段（00:30 更新）；当前无在用用户，变更仍须评估影响 |
| M1X | `m1x-api` **1/1**，`m1x-worker` **1/1** | `m1x-api.svision100.com` | API、Worker 是独立镜像和 Deployment，均已迁入新出口网段（00:30 更新）；当前无在用用户 |
| 新零售 | Front/Back/Worker 均 **1/1** | `ns-front-api.svision100.com`、`ns-back-api.svision100.com` 的 `/health/ready` 均 200 | 业务外呼、支付和任务结果未由本次核对 |
| 经销商查询 | 单体 `agent-query-api` **1/1**，无 Worker | `4l-api.svision100.com/health/ready` 200 | 前后台同一个 API；历史 `rsqapi-ft/bk` 不是本次入口 |
| AI 自习室 | `ai-study-back` **1/1**，`ai-study-worker` **1/1** | `rsst-back-api.svision100.com/health/ready` 200；FrontApi 留 ECS | 两个 ACS Pod 位于新出口网段；真实短信及任务结果待业务验收 |
| 售后工单 | Front/Back/Worker 均 **1/1** | `rsod-front-api`、`rsod-back-api.svision100.com/health/ready` 均 200 | **Worker 已于 2026-09-27 00:21 启动**（新网段 `172.31.240.149`），定时任务正常执行；业务结果与 24 小时观察待验收 |
| 积分商城 | Back **1/1**，Worker **1/1**；ACS Front 尚未部署 | `rsjf-back-api.svision100.com/health/ready` 200；旧 FrontApi 继续 ECS | 旧 Front 与 ACS Back/Worker 分工；Worker 容器 0 次重启，定时任务业务结果待验收 |

本次读到的运行中业务 Pod 均为 `Running`、重启数 0。DGYE Namespace 还留有历史失败诊断 Pod，勿把它当作运行中的 DGYE API。本次批量核对到 AI、售后、积分、经销商四个旧 .NET ECS 实例状态为 `Running`；**ECS Running 不证明其中每个 systemd 服务在运行**，也不证明 Worker 已停。新零售旧 ECS 在这次批量响应中未返回，按未知处理，不据此宣称已停或已释放。

## 网络和私网边界

- 新出口 Pod vSwitch：北京 k `vsw-2zevd832gq3313j6gv5sj` / `172.31.240.0/24`；北京 i `vsw-2zesy6off4gy39tqripzp` / `172.28.64.0/24`。2026-09-26 23:39 读回两条 SNAT 均 `Available`，指向 `39.96.67.239`。AI Back/Worker、售后 Front/Back/Worker、积分 Back/Worker、**Yangu、M1X API/Worker**（00:30 更新）已在新网段；STOPMP、ETBST、DDMP、新零售及经销商当前 Pod IP 仍在旧网段。
- 旧七个 vSwitch 走 `vtb-2ze9057j04btdfr3bofhy`，无默认路由；新两个 Pod vSwitch 走 `vtb-2zedknif7nxu7z397b4o6` 并指向 NAT。**不要将旧 vSwitch 直接切回已有 NAT 默认路由的系统表**。实际拓扑和 RequestId 见[网络实施记录](egress-nat-execution-2026-09-26.md)。
- 新网段到 MySQL、Redis、MongoDB、SmsCore 的网络准备曾做双区 TCP 验证；当前各项目认证、实际 SQL/Redis/Mongo 操作及公网 SDK 来源 IP 并未由本次交接核验。Mongo 对端路由/白名单、OpenVPN 边界见[网段记录](network-constraints-2026-09-26.md)。本机 OpenVPN 不保证能直接访问新 Pod IP。
- SmsCore 私网是**业务 Pod 到短信中心**的路径；短信中心 ECS 到供应商仍走自身公网出口。不得把新 NAT EIP填进短信供应商的 SmsCore 出口白名单，除非先从供应商侧实测需要。

## 当前必须交接的未完成事项

1. ~~售后工单 Worker 目前 0 副本。~~ 已于 2026-09-27 00:21 启动（1/1，定时任务正常），见[变更记录](worker-start-and-java-egress-2026-09-27.md)；真实业务结果与 24 小时观察仍待验收。
2. 积分商城旧 FrontApi 留 ECS，ACS Back 与 Worker 已运行。切勿误将旧 Front 当作旧 Worker 停掉。前端消费者、Worker 定时任务与业务幂等的运行结果仍待核对。
3. AI 旧 FrontApi 留 ECS；新 Back/Worker 与旧 Front 共用业务环境的兼容、微信 token 和短信实际投递尚缺本次验收证据。
4. 七个 Java 项目是否有仍启用的旧直连短信供应商通道、各项目真实公网 SDK 出口和供应商 IP 白名单，尚不能凭网络读回定论。旧网段没有本次 NAT SNAT；迁移须逐项目安排。
5. 正式域名健康检查已通过，但登录、订单、积分、退款、ERP、短信回执、首次到期任务和 24 小时观察尚需业务证据。资金/短信写入测试不要用 HTTP 200 代替。
6. 运维告警接收渠道、NAT/EIP 实账、各项目配置版本与源代码 SHA 的完整发布清单尚未统一核验；在完成前不要把此快照当成审计完成证明。

## 接手顺序

1. 按 [API 使用说明](aliyun-api-operations.md)验证生产身份并重新读回 Deployment、Ingress、NAT、EIP、SNAT；勿使用历史临时 kubeconfig。
2. 对照本页找变化，先定位对象和时间，再查看目标 Pod 事件/有限日志、旧 ECS 的具体服务与数据库/供应商记录。不要用 ECS 实例 `Running` 推断某个服务状态。
3. 按[项目发布记录](release-records.md)查镜像、配置及已做过的验收；历史“预部署”段落仅代表当时快照。新增证据写明时间、对象、RequestId/资源 UID和结论。
4. 变更前先保存对象现状；生产配置、数据库、权限、付费网络和切流需确认当前授权范围。回滚单个项目只动该项目入口/Deployment，避免撤销共享 NAT 影响其他运行项目。

本交接页面只做记录，没有启动 Worker、发短信、处理资金、修改云资源或读取 Secret 值。
